language: php

php:
  - 5.5.9
  - 5.6
  - 7.0
  - hhvm

matrix:
  fast_finish: true
  allow_failures:
    - php: hhvm

install:
  - composer install --no-interaction

script: 
  - php -i
  - vendor/bin/phpunit --coverage-text
